const Resume = () => (
    <section className="resume">
        <div className="resume__skills">
            skills in some grid maybe
        </div>
        <div className="resume_experience">
            experience/resume
        </div>
    </section>
);

export default Resume;